#!/bin/bash
# Syscheck-upstairs: checks folders above the home directory
#   - disk space (/, /home)
#   - /tmp cleanup needs
#   - /var/log size
#   - unexpected users in /home
# Usage: syscheck-upstairs [--clean]  (--clean clears old tmp/log/apt files)
# Location: ~/bin/syscheck-upstairs

THRESHOLD_PCT=80
TMP_DAYS=7
LOG_DAYS=30
CACHE_DAYS=30

ALERTS=()
NOTES=()

echo "── syscheck-upstairs ──────────────────────────────"
echo ""

# ── 1. Disk space ──
echo "📁 Disk space:"
while IFS= read -r line; do
  mount=$(echo "$line" | awk '{print $6}')
  size=$(echo "$line" | awk '{print $2}')
  used=$(echo "$line" | awk '{print $3}')
  pct=$(echo "$line" | awk '{print $5}')
  pct_num=${pct%\%}
  echo "  $mount  $used / $size  (${pct} used)"
  if [ "$pct_num" -ge "$THRESHOLD_PCT" ]; then
    ALERTS+=("WARNING: $mount is $pct full!")
  fi
done < <(df -h / /home 2>/dev/null | tail -n +2)

# ── 2. /tmp ──
echo ""
echo "🗑️  /tmp:"
tmp_size=$(du -sh /tmp 2>/dev/null | cut -f1)
tmp_old=$(find /tmp -maxdepth 1 -atime +$TMP_DAYS -not -name '.X*-lock' -not -name '.ICE-unix' 2>/dev/null | wc -l)
echo "  Size: $tmp_size"
echo "  Files older than $TMP_DAYS days: $tmp_old"
NOTES+=("/tmp: $tmp_old old files")

# ── 3. /var/log ──
echo ""
echo "📋 /var/log:"
log_size=$(du -sh /var/log 2>/dev/null | cut -f1)
log_old=$(find /var/log -type f -mtime +$LOG_DAYS 2>/dev/null | wc -l)
echo "  Size: $log_size"
echo "  Logs older than $LOG_DAYS days: $log_old"
NOTES+=("/var/log: $log_size, $log_old old logs")

# ── 4. /var/cache/apt/archives ──
echo ""
echo "📦 APT-cache:"
cache_size=$(du -sh /var/cache/apt/archives 2>/dev/null | cut -f1)
cache_old=$(find /var/cache/apt/archives -name '*.deb' -mtime +$CACHE_DAYS 2>/dev/null | wc -l)
echo "  Size: $cache_size"
echo "  .deb older than $CACHE_DAYS days: $cache_old"
NOTES+=("APT-cache: $cache_size")

# ── 5. Unknown home dirs ──
echo ""
echo "👤 Other users in /home:"
other_users=0
for dir in /home/*/; do
  user=$(basename "$dir")
  if [ "$user" != "$USER" ]; then
    echo "  ⚠️  $user"
    ALERTS+=("Unknown user in /home: $user")
    other_users=$((other_users + 1))
  fi
done
if [ "$other_users" -eq 0 ]; then
  echo "  None (only $USER)"
fi

# ── 6. Optional --clean ──
if [ "$1" = "--clean" ]; then
  echo ""
  echo "🧹 Cleaning up old files..."
  if [ "$tmp_old" -gt 0 ]; then
    find /tmp -maxdepth 1 -atime +$TMP_DAYS -not -name '.X*-lock' -not -name '.ICE-unix' -exec rm -rf {} \; 2>/dev/null
    echo "  /tmp cleaned up"
  fi
  if [ "$log_old" -gt 0 ]; then
    sudo find /var/log -type f -mtime +$LOG_DAYS -exec rm -f {} \; 2>/dev/null && echo "  /var/log cleaned up" || echo "  /var/log: sudo required"
  fi
  if [ "$cache_old" -gt 0 ]; then
    sudo apt-get autoclean -q 2>/dev/null && echo "  APT-cache cleaned up" || echo "  APT autoclean: sudo required"
  fi
fi

echo ""
echo "── Summary ────────────────────────────────────────"
for note in "${NOTES[@]}"; do
  echo "  • $note"
done
echo ""
if [ ${#ALERTS[@]} -gt 0 ]; then
  echo "⚠️  ALERTS:"
  for alert in "${ALERTS[@]}"; do
    echo "  ⚠️  $alert"
  done
else
  echo "✅ All OK — nothing unusual"
fi
